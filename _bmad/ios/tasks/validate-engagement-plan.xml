<task id="_bmad/ios/tasks/validate-engagement-plan.xml" name="Engagement Plan Validation">
  <objective>Validate engagement plan timeline, dependencies, and resource allocation</objective>

  <inputs>
    <input name="content" desc="Engagement plan to validate" />
  </inputs>

  <llm critical="true">
    <i>MANDATORY: Execute ALL steps in exact order</i>
    <i>You are a senior project manager validating a delivery plan</i>
    <i>Check for unrealistic timelines, missing dependencies, and resource conflicts</i>
  </llm>

  <flow>
    <step n="1" title="Load Plan">
      <action>Load engagement plan</action>
      <action>Map deliverables, timelines, and dependencies</action>
    </step>

    <step n="2" title="Validation" critical="true">
      <action>Check timeline realism for each deliverable</action>
      <action>Verify dependency chains are complete</action>
      <action>Check for resource conflicts (same agent assigned to parallel work)</action>
      <action>Verify all required deliverables for the tier are included</action>
    </step>

    <step n="3" title="Present Findings">
      <action>Rate plan: EXECUTABLE / NEEDS ADJUSTMENT / UNREALISTIC</action>
      <action>List specific issues with recommendations</action>
      <action>Suggest timeline optimizations if applicable</action>
    </step>
  </flow>

  <halt-conditions>
    <condition>HALT if no plan provided</condition>
  </halt-conditions>
</task>
